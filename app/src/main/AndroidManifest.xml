<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_app"
        android:label="@string/app_name"

        android:networkSecurityConfig="@xml/network_security_config"

        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppPlant"
        tools:targetApi="31">

        <activity
            android:name=".ThanhToan"
            android:exported="false"
            android:theme="@style/Theme.AppPlant.NoActionBar"/>

        <activity
            android:name=".ui.history.HistoryFragment"
            android:exported="false" />

        <activity
            android:name=".ui.profile.info"
            android:exported="false" />
        <activity
            android:name=".ui.profile.verify_email"
            android:exported="false" />
        <activity
            android:name=".inf_sp.inf_sp"
            android:exported="false"
            android:theme="@style/Theme.AppPlant.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.AppPlant.NoActionBar" />
        <activity
            android:name=".SignUp"
            android:exported="false"
            android:theme="@style/Theme.AppPlant.NoActionBar" />

        <activity
            android:name=".Start"
            android:exported="false"
            android:theme="@style/Theme.AppPlant.NoActionBar" />
        <activity
            android:name=".Login"
            android:exported="true"
            android:theme="@style/Theme.AppPlant.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>